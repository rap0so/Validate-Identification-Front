import styled from 'styled-components'
import { bodyButton } from '../ButtonAddNewId/ButtonAddnewId.style'

export default styled.button`
    ${bodyButton}
    padding: 10px 17px
    img {
        filter: invert(1);
    }
`
